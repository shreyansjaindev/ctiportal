# Frontend - Development (local machine, dev server proxies to http://localhost:8000)
# Use with: npm run dev

# API base URL (dev server proxies /api/* to VITE_API_PROXY_TARGET)
VITE_API_PROXY_TARGET=http://localhost:8000

# Optionally override API base (if using a remote backend during dev)
# VITE_API_BASE_URL=http://localhost:8000/api/v1
