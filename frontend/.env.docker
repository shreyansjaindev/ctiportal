# Frontend - Docker Compose (containers, backend service name resolution)
# Use with: docker-compose up
# Note: vite.config.ts reads VITE_API_PROXY_TARGET for dev proxy setup

VITE_HOST=0.0.0.0
VITE_PORT=5173
VITE_API_PROXY_TARGET=http://backend:8000
