# Backend - Production (Render deployment)
# WARNING: Use Render's native environment variable UI to set these, NOT a committed file.
# This is a TEMPLATE showing which variables must be configured.
# DO NOT commit actual secrets to git.

# Django (Render auto-provides SECRET_KEY via generateValue: true in render.yaml)
SECRET_KEY=__AUTO_GENERATED_BY_RENDER__
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app,https://your-render-domain.onrender.com
CSRF_TRUSTED_ORIGINS=https://your-frontend-domain.vercel.app,https://your-render-domain.onrender.com
JWT_ACCESS_MINUTES=60
JWT_REFRESH_DAYS=7

# Database (Render Postgres instance)
# Render auto-fills these from the linked database service in render.yaml
DB_ENGINE=django.db.backends.postgresql
DB_NAME=__FROM_RENDER_DATABASE__
DB_USER=__FROM_RENDER_DATABASE__
DB_PASSWORD=__FROM_RENDER_DATABASE__
DB_HOST=__FROM_RENDER_DATABASE__
DB_PORT=__FROM_RENDER_DATABASE__

# Third-party API keys (optional, only if you want to enable those integrations)
IPAPI=
WHOISXMLAPI_DRS=
WHOISXMLAPI_NRD_FEED=
VIRUSTOTAL=
ANOMALI=
URLSCAN_IO=
WEBSCRAPING_AI=
PHISHTANK=
PULSEDIVE=
SENDGRID=
SECURITYTRAILS=
SCREENSHOTMACHINE=
SCREENSHOTLAYER=
ABUSEIPDB=
APILAYER=
IBM_XFORCE=
HYBRIDANALYSIS=
HOST_IO=
SITERELIC=
API_NINJAS=
APILITY=
PROOFPOINT_CLIENT_ID=
PROOFPOINT_CLIENT_SECRET=
PROOFPOINT_CLUSTER_ID=
ORG_NAME=
TRELLIX=
TRELLIX_RULE_NAME=
TRELLIX_POLICY_UUID=
